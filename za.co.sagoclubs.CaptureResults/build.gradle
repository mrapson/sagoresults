plugins {
    id 'com.android.application'
}

def secretPropertiesFile = rootProject.file("secret.properties")
def secretProperties = new Properties()
secretProperties.load(new FileInputStream(secretPropertiesFile))

android {
    namespace 'za.co.sagoclubs'
    compileSdk 34
    buildFeatures {
        buildConfig true
    }

    defaultConfig {
        applicationId "za.co.sagoclubs"
        minSdk 33
        targetSdk 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        buildConfigField("String", "SAGA_WP_SITE", secretProperties['SAGA_WP_SITE'])
        buildConfigField("String", "RANK_SITE", secretProperties['RANK_SITE'])
        buildConfigField("String", "COGNITO_POOL_ID", secretProperties['COGNITO_POOL_ID'])
        buildConfigField("String", "COGNITO_CLIENT_ID", secretProperties['COGNITO_CLIENT_ID'])
        buildConfigField("String", "COGNITO_CLIENT_SECRET", secretProperties['COGNITO_CLIENT_SECRET'])
        buildConfigField("String", "COGNITO_AWS_REGION", secretProperties['COGNITO_AWS_REGION'])
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.debug
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
        }
    }
}

dependencies {
    implementation 'com.amazonaws:aws-android-sdk-core:2.7.4'
    implementation 'com.amazonaws:aws-android-sdk-cognitoidentityprovider:2.7.0'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'com.google.android.material:material:1.12.0'
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.2.1'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.6.1'
    implementation 'org.jsoup:jsoup:1.18.1'
}